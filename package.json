{
  "scripts": {
    "build": "yarn spago build",
    "build-watch": "yarn spago build -w",
    "repl": "yarn spago repl"
  },
  "devDependencies": {
    "purescript": "^0.13.3",
    "purty": "^4.5.1",
    "spago": "^0.10.0"
  }
}
